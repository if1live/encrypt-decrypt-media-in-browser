{
  "name": "encrypt-decrypt-media-in-web",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "parcel index.html --out-dir docs --public-url /",
    "build": "parcel build index.html --out-dir docs/ --public-url ./"
  },
  "dependencies": {
    "aes-js": "^3.1.0"
  },
  "devDependencies": {
    "parcel-bundler": "^1.6.2"
  }
}
